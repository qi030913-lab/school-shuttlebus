server:
  port: 8080

spring:
  application:
    name: shuttle-demo-v3
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/shuttle_demo?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 030913
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always
      encoding: UTF-8
      schema-locations: classpath:db/mysql/schema.sql
      data-locations: classpath:db/mysql/demo-data.sql

wechat:
  miniapp:
    app-id: your-miniapp-appid
    secret: your-miniapp-secret
    mock-login-enabled: true
    session-expire-days: 7
